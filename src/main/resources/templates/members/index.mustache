{{>layouts/header}}

<table class="table">
    <thead>
    <tr>
        <th scope="col">Id</th>
        <th scope="col">Email</th>
        <th scope="col">Password</th>
    </tr>
    </thead>
    {{#memberList}} <!-- 컨트롤러에서 선언한 변수의 사용범위를 설정 -->
        <tr>
            <th>{{id}}</th>
            <td><a href="/members/{{id}}">{{email}}</a></td>
            <td>{{password}}</td>
        </tr>
    {{/memberList}}

    </tbody>
</table>
<a href="/join" class="btn btn-warning">회원가입</a>
{{>layouts/footer}}